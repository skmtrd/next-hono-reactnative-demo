# Next.js Frontend

Next.js を使用したフロントエンドアプリケーションです。
Hono API サーバーと通信します。

## セットアップ

```bash
npm install
```

## 開発サーバーの起動

```bash
npm run dev
```

フロントエンドは `http://localhost:3000` で起動します。

## 注意

このフロントエンドを使用する前に、Hono API サーバー（`http://localhost:8787`）を起動してください。
